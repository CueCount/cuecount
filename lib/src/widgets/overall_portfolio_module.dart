import 'package:flutter/material.dart';

class OverallPortfolioModule extends StatelessWidget {
  const OverallPortfolioModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
      child: const Center(
        child: Text('Your Overall Portfolio'),
      ),
    );
  }
}
