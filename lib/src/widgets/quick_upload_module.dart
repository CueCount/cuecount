import 'package:flutter/material.dart';

class QuickUploadModule extends StatelessWidget {
  const QuickUploadModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.teal,
      child: const Center(
        child: Text('Quick Upload Document'),
      ),
    );
  }
}
